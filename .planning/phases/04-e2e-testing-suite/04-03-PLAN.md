---
phase: 04-e2e-testing-suite
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [
  "apps/web/e2e/streaming.spec.ts",
  "apps/web/e2e/forensic_ui.spec.ts",
  "apps/web/e2e/security.spec.ts"
]
autonomous: true
requirements: [UX-01, ORCHESTRATION-01, SECURITY-01]
must_haves:
  truths:
    - "SSE streaming correctly renders partial updates in the UI"
    - "Chat streaming behaves as expected for long agent responses"
    - "Forensic banners appear when the auditor flags a conflict"
    - "User A cannot access User B's case data via URL direct access"
  artifacts:
    - path: "apps/web/e2e/streaming.spec.ts"
      provides: "SSE and Chat streaming E2E tests"
    - path: "apps/web/e2e/forensic_ui.spec.ts"
      provides: "UI component and forensic banner verification"
  key_links:
    - from: "Playwright"
      to: "Next.js SSE Endpoint"
      via: "Network interception"
---

<objective>
Verify the user-facing functionality of the KuratorMind AI platform using Playwright. This plan focuses on testing the reactive UI, real-time streaming components, and security boundaries (RLS).

Purpose: User confidence is built on a responsive and secure interface.
Output: Suite of Playwright E2E tests for the frontend.
</objective>

<execution_context>
@/Users/muflihiskandar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/muflihiskandar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@apps/web/src/app/(app)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE & Chat Streaming E2E Tests</name>
  <files>apps/web/e2e/streaming.spec.ts</files>
  <action>
    - Create `streaming.spec.ts` to test real-time agent responses.
    - Test SSE ingestion progress bar updates.
    - Test the Chat streaming interface: send a prompt and verify the word-by-word rendering in the UI.
    - Intercept network calls to simulate slow streaming and verify UI stability.
  </action>
  <verify>
    - `npx playwright test apps/web/e2e/streaming.spec.ts` passes.
    - Test confirms that text appears incrementally during the chat stream.
  </verify>
  <done>Streaming orchestration is verified from the user perspective.</done>
</task>

<task type="auto">
  <name>Task 2: Forensic UI & Banner Verification</name>
  <files>apps/web/e2e/forensic_ui.spec.ts</files>
  <action>
    - Create `forensic_ui.spec.ts` to verify high-stakes UI components.
    - Verify that the "Conflict" banner appears on the Dashboard when an audit flag is present (e.g., Sritex BCA case).
    - Verify the "Audit Flag" cards in the `ClaimsTab` correctly display the forensic reasoning.
    - Verify that clicking a flag correctly navigates to the source PDF line in `SourcesTab` (if implemented).
  </action>
  <verify>
    - `npx playwright test apps/web/e2e/forensic_ui.spec.ts` passes.
    - Banners and flag cards are visible when test data is loaded.
  </verify>
  <done>Forensic banners and UI components are functional and correctly integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Security & RLS Boundary Testing</name>
  <files>apps/web/e2e/security.spec.ts</files>
  <action>
    - Create `security.spec.ts` to verify Supabase RLS and Next.js middleware protection.
    - Test Case 1: Unauthorized user redirected to login when accessing `/dashboard`.
    - Test Case 2: Authenticated User B attempts to access `/case/[userA_id]` and receives a 403 or redirect.
    - Verify that no sensitive keys (like Supabase service role) are present in the frontend build or Playwright traces.
  </action>
  <verify>
    - `npx playwright test apps/web/e2e/security.spec.ts` passes all security scenarios.
  </verify>
  <done>Multi-tenant security boundaries are strictly enforced and verified.</done>
</task>

</tasks>

<verification>
- Run all E2E tests: `npx playwright test`.
- Run a final verification of the full pipeline (Seeding -> Backend Tests -> E2E Tests).
</verification>

<success_criteria>
- All Playwright E2E tests pass.
- Streaming components render correctly without flickering or state loss.
- Security boundaries prevent unauthorized data access.
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-testing-suite/04-03-SUMMARY.md`
</output>
