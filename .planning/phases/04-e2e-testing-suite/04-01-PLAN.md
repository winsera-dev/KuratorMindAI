---
phase: 04-e2e-testing-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "apps/agents/pyproject.toml",
  "apps/web/package.json",
  "apps/agents/tests/conftest.py",
  "tests/scripts/seed_test_data.py",
  "tests/scripts/ocr_benchmark.py"
]
autonomous: true
requirements: [OCR-01, DATA-01]
must_haves:
  truths:
    - "Test environment can be reset to a clean state automatically"
    - "OCR accuracy is measured in CER/WER using the Golden Dataset"
    - "Playwright can interact with the Next.js frontend"
  artifacts:
    - path: "tests/scripts/seed_test_data.py"
      provides: "Supabase environment reset and seeding logic"
    - path: "tests/scripts/ocr_benchmark.py"
      provides: "JiWER integration for CER/WER calculation"
  key_links:
    - from: "Pytest"
      to: "Supabase"
      via: "Python Supabase Client in conftest.py"
---

<objective>
Initialize the testing infrastructure and baseline data quality measurements. This plan sets up the environment and implements OCR benchmarking to ensure data ingestion integrity.

Purpose: Without high OCR accuracy, downstream audit and compliance logic will fail ("Garbage In, Garbage Out").
Output: Configured test frameworks and baseline OCR metrics.
</objective>

<execution_context>
@/Users/muflihiskandar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/muflihiskandar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
@tests/data/test_sritex_claims.csv
@tests/data/Daftar_Piutang_Sritex_Simulasi_2025.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testing Environment & Tooling Setup</name>
  <files>apps/agents/pyproject.toml, apps/web/package.json</files>
  <action>
    - Add test dependencies to `apps/agents/pyproject.toml`: `jiwer`, `splink`, `eyecite`, `faker`, `langfuse`, `pytest-asyncio`, `pytest-env`.
    - Add `@playwright/test` to `apps/web/package.json`.
    - Install dependencies and run `playwright install`.
    - Create `apps/agents/pytest.ini` and `apps/web/playwright.config.ts` with basic configurations.
  </action>
  <verify>
    - `pytest --version` returns 8.x
    - `npx playwright --version` returns current version
    - Both tools can run a simple "hello world" test.
  </verify>
  <done>Testing tools are installed and basic configuration files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Database Seeding Engine & Supabase Fixtures</name>
  <files>apps/agents/tests/conftest.py, tests/scripts/seed_test_data.py</files>
  <action>
    - Create `tests/scripts/seed_test_data.py` to reset Supabase buckets, clear tables, and seed initial test cases (Sritex, Mitralanggeng).
    - Create `apps/agents/tests/conftest.py` with Pytest fixtures for `supabase_client` and `auth_headers`.
    - Use `SUPABASE_SERVICE_ROLE_KEY` (if available in env) to bypass RLS for seeding.
  </action>
  <verify>
    - Running `python tests/scripts/seed_test_data.py` successfully resets the database to the known test state.
    - Pytest can connect to Supabase and query tables.
  </verify>
  <done>The database can be reset to a deterministic state for testing.</done>
</task>

<task type="auto">
  <name>Task 3: OCR Benchmark Suite (JiWER)</name>
  <files>tests/scripts/ocr_benchmark.py</files>
  <action>
    - Implement `ocr_benchmark.py` using `jiwer` to calculate CER (Character Error Rate) and WER (Word Error Rate).
    - Use the "Golden Dataset" in `tests/data/` (Sritex CSV as ground truth for Sritex PDF).
    - Compare OCR output from `apps/agents/services/ingestion.py` against the ground truth.
    - Output a summary report with metrics for each document.
  </action>
  <verify>
    - `python tests/scripts/ocr_benchmark.py` outputs CER/WER for at least one PDF in `tests/data/`.
    - CER is below 5% for the high-quality simulasi PDF.
  </verify>
  <done>OCR accuracy is quantitatively measured and reported.</done>
</task>

</tasks>

<verification>
- Run `pytest` on agents to ensure environment is correct.
- Run `npx playwright test` on web to ensure environment is correct.
- Run the seeding script and verify database state in Supabase dashboard.
- Run the OCR benchmark and verify output report.
</verification>

<success_criteria>
- Testing frameworks (Pytest, Playwright) fully initialized.
- Database seeding script functional.
- OCR CER/WER metrics generated for baseline documents.
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-testing-suite/04-01-SUMMARY.md`
</output>
