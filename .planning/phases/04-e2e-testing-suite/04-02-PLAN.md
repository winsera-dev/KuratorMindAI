---
phase: 04-e2e-testing-suite
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [
  "apps/agents/tests/test_compliance.py",
  "apps/agents/tests/test_audit_logic.py",
  "apps/agents/tests/test_entity_resolution.py"
]
autonomous: true
requirements: [LEGAL-01, AUDIT-01, ENTITY-01]
must_haves:
  truths:
    - "System validates UU 37/2004 triggers (creditor plurality, debt maturity)"
    - "Claim auditor flags discrepancies in Sritex BCA case correctly"
    - "Creditor 'Heryanto' is correctly resolved across different test cases"
  artifacts:
    - path: "apps/agents/tests/test_compliance.py"
      provides: "Bankruptcy compliance trigger validation tests"
    - path: "apps/agents/tests/test_audit_logic.py"
      provides: "Financial audit discrepancy detection tests"
  key_links:
    - from: "test_entity_resolution.py"
      to: "Splink"
      via: "Probabilistic record linkage"
---

<objective>
Implement core functional tests for legal compliance, forensic audit, and cross-case entity resolution. This ensures the system correctly applies the laws of UU 37/2004 and accurately links financial data.

Purpose: Legal logic and forensic auditing must be 100% accurate for platform credibility.
Output: Suite of Pytest integration tests for backend logic.
</objective>

<execution_context>
@/Users/muflihiskandar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/muflihiskandar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@tests/data/test_sritex_claims.csv
@apps/agents/kuratormind/agents/claim_auditor/agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: UU 37/2004 Legal Compliance Testing</name>
  <files>apps/agents/tests/test_compliance.py</files>
  <action>
    - Create `test_compliance.py` to validate automated PKPU/Bankruptcy triggers (Article 2 & 8 of UU 37/2004).
    - Test Case 1: Multiple creditors (2+) and at least one matured debt → SUCCESS trigger.
    - Test Case 2: Only one creditor → FAIL trigger.
    - Test Case 3: Multiple creditors but no matured debt → FAIL trigger.
  </action>
  <verify>
    - `pytest apps/agents/tests/test_compliance.py` passes all 3 scenarios.
  </verify>
  <done>UU 37/2004 automated compliance gates are verified and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Forensic Audit Discrepancy Detection (Sritex Case)</name>
  <files>apps/agents/tests/test_audit_logic.py</files>
  <action>
    - Create `test_audit_logic.py` to test the `claim_auditor` agent using `test_sritex_claims.csv`.
    - Verify that the agent flags discrepancies between "Claimed Amount" and "Bank Statement Amount" (using Benford's law and duplicate transaction detection).
    - Assert that specific known conflicts in the Sritex BCA scenario are identified.
  </action>
  <verify>
    - `pytest apps/agents/tests/test_audit_logic.py` identifies discrepancies in the provided test CSV.
  </verify>
  <done>Financial audit logic correctly flags discrepancies in the Sritex test data.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-Case Entity Resolution Benchmarks (Heryanto Scenario)</name>
  <files>apps/agents/tests/test_entity_resolution.py</files>
  <action>
    - Create `test_entity_resolution.py` using `Splink` and `eyecite` for entity resolution testing.
    - Setup two test cases in Supabase with overlapping creditors (e.g., "Heryanto" appearing in both).
    - Verify the "Heryanto" is linked to the same Global Identity with a match score > 0.85.
    - Verify that names with slight typos (e.g., "Heriyanto" vs "Heryanto") are resolved to the same entity.
  </action>
  <verify>
    - `pytest apps/agents/tests/test_entity_resolution.py` resolves the Heryanto scenario with high confidence.
  </verify>
  <done>Cross-case entity linking is automated and resolved correctly across vaults.</done>
</task>

</tasks>

<verification>
- Run the entire backend test suite using `pytest apps/agents/tests`.
- Verify the test coverage using `pytest --cov`.
- Ensure all tests are running against the Supabase test environment seeded in Plan 01.
</verification>

<success_criteria>
- Legal compliance tests pass for UU 37/2004.
- Forensic audit flags are accurately identified for known data conflicts.
- Entity resolution resolves creditor identities across cases with typos handled.
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-testing-suite/04-02-SUMMARY.md`
</output>
